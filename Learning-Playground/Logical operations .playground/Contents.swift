//: Playground - noun: a place where people can play

import UIKit

var anyVariable: Int
var pi = 3.14
anyVariable = pi == 3.14 ? 1234 : 5678

var hasKey = false
var hasPassword = false
var isTheOwner = true
if hasKey && hasPassword || isTheOwner {
    print("No matter")
}

print(hasKey) //it is possible to print boolean objects
